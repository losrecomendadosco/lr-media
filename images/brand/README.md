# Brand assets

Store approved logos, marks, icons, fonts, and other identity assets here.

Use descriptive filenames and preserve the source format when possible (for example, SVG for vector logos and PNG for transparent raster assets).
